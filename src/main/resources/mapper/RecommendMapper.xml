<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jk.mapper.RecommendMapper">
 <resultMap type="com.jk.model.Recommend" id="BaseMap">
 	<result column="id" property="id" jdbcType="BIGINT"/>
 	<result column="type" property="type" jdbcType="INTEGER"/>
 	<result column="recommed_id" property="recommed_id" jdbcType="BIGINT"/>
 	<result column="image_filename" property="image_filename" jdbcType="VARCHAR"/>
 	<result column="create_time" property="createTime" jdbcType="TIMESTAMP"/>
 	<result column="modify_time" property="modifyTime" jdbcType="TIMESTAMP"/>
 </resultMap>
 
  <select id="recommendList" resultMap="BaseMap" parameterType="map">
	  select * from t_recommend 
	  <where> 
		  <if test="findtype != null and findtype!=''">
		      type = #{findtype}
		  </if> 
	  </where>
	  order by type asc, order_number asc
  </select>

</mapper>